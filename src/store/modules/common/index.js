

const defaultState = {
  focused: false,
  mouseIn: false,
  list: [],
  showScroll: true
};

export default (state = defaultState, action) => {
  switch (action.type) {
    default: return state;
  }
}